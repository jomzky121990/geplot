GE Plot v5.3b — Rebuild (tolerant bearing, Google Satellite default, Windows-safe shapefile append).
Run:
  python -m venv .venv
  .venv\Scripts\activate  (Windows)  OR  source .venv/bin/activate  (macOS/Linux)
  pip install -r requirements.txt
  streamlit run GE_Plot_v53b.py
